using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArcherEnemy : MonoBehaviour
{
    // Start is called before the first frame update

    //ANIMATIONS ETC WILL GO HERE.
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
